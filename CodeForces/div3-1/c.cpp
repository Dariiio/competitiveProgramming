#include <bits/stdc++.h>
using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	//fropen("input.txt","r",stdin);
	long int n,k,x;
	cin>>n>>k;
	long int v[n];
	for(int i=0;i<n;i++){
		cin>>v[i];
	}
	x=1;
	for(int i=0;i<n;i++){
		
	}
	return 0;
}