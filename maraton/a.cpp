#include<bits/stdc++.h>
using namespace std;

long long t,n;
long long left,right;

int main()
{
	//ios::sync_with_stdio(false);
	//cin.tie(NULL);
	//freopen("input.txt","r",stdin);
	cin>>t;
	while(t--){
		left=0;right=1;
		cin>>n;
		left+=n;
		while(right<left){
			right*=3
		}
		if(left<right){
			left
		}
	}
	return 0;
}